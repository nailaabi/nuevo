// crea una calculadora 
const prompt =  require ('prompt-sync')();

const num1 = Number (prompt ( 'Ingresa una numero: '))
const num2 = Number (prompt ( 'Ingresa otronumero: '))

const operacion = prompt ('ingrese una operacion '( +, -, * , / ));

const